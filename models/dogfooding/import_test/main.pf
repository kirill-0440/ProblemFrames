problem: ImportTest

import "hardware.pf"

domain Controller kind causal role machine

interface "Controller-Sensor" connects Controller, Sensor {
    shared: {
        phenomenon read : event [Sensor -> Controller] controlledBy Sensor
    }
}
