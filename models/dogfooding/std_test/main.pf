problem: StdTest
import "std/RequiredBehavior.pf"

// "Machine" and "ControlledDomain" should be available from std lib
interface "Operator-Machine" connects Operator, Machine {
    shared: {
        phenomenon start : event [Operator -> Machine] controlledBy Operator
    }
}

domain Operator kind biddable role given
