# Models

This directory stores first-class Problem Frames models used as repository-level
artifacts.

- `system/`: canonical PF model of the ProblemFrames toolchain itself.

Model gates can be executed with:

```bash
bash ./scripts/check_system_model.sh
```
