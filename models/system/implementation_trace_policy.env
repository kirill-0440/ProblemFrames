# Staged implementation-trace policy for system-model quality checks.
# Counts are evaluated against the current model requirement set.

# Disallow partially or not-yet-implemented requirements in the canonical model.
TRACE_POLICY_MAX_PARTIAL=0
TRACE_POLICY_MAX_PLANNED=0

# Additional hard guard for delivery-track requirements.
TRACE_POLICY_BLOCKING_PREFIXES=R005,R006,R007,R008,R009,R010
TRACE_POLICY_MAX_BLOCKING_OPEN=0
